1, kết nối database 
   url: jdbc:postgresql://localhost:5432/wareHouse
   username: wareHouse
   password: 1234

2, có thuật toán xử lý ô gộp rất hay trong code 

3, lệnh chạy chương trình cho cả BE và FE : ./mvnw 
   lệnh chạy phần BE : click nút run 
   lệnh chạy phần FE : ng serve ( bên VS code ) hay ./npmw run webapp:dev
